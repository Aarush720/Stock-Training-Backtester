{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "python3 -m pip install -r requirements-vercel.txt && npm --prefix frontend install",
  "buildCommand": "npm --prefix frontend run build:client",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/run-backtest/index.py": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}